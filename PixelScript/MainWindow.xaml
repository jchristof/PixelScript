<Window x:Class="PixelScript.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PixelScript"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        Title="PixelScript" Height="450" Width="800"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Image x:Name="Image" Margin="1" Stretch="Uniform" VerticalAlignment="Center"></Image>
        <Button Grid.Row="1" Margin="1" Click="Select_OnClick">Select</Button>
        <Button Grid.Row="2" Margin="1"  Click="Process_OnClick">Process</Button>
        <Button Grid.Row="3" Margin="1"  Click="Exit_OnClick">Exit</Button>
    </Grid>
</Window>
